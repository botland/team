{
  "type": "object",
  "required": ["answers_markdown"],
  "properties": {
    "answers_markdown": { "type": "string" },
    "census_markdown": {
      "type": "string",
      "description": "Tree inventory if census.md is still missing. Do not recensus when it exists."
    }
  }
}
