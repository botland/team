{
  "type": "object",
  "required": ["ready", "consult", "questions"],
  "properties": {
    "ready": { "type": "boolean" },
    "consult": { "type": "string" },
    "questions": {
      "type": "array",
      "maxItems": 10,
      "items": { "type": "string" }
    },
    "summary": { "type": "string" },
    "census_markdown": {
      "type": "string",
      "description": "Tree inventory if census.md is still missing. Do not recensus when it exists."
    }
  }
}
