# Open classes

The register of **classes**, not of instances. A row closes on a named
mechanism at rung 1–2 (unrepresentable, or delegated to the layer that owns
the space); a census or a contract test marks it **guarded**, which is a state
that stays open. Never append a status line to a row — edit it or delete it.

Rungs: 1 unrepresentable · 2 delegate to the owning layer · 3 one authority +
census · 4 contract test across a seam · 5 review/doc/convention.

Round of 2026-08-16, against the review of `fd3021f` (`/tmp/review/claude`).

| # | Class | Rung reached | Mechanism | Open? |
|---|---|---|---|---|
| L1 | Claude tool filters cannot express an allowlist | 5 + boundary | Under `--permission-mode acceptEdits` an `Edit(root/**)` entry pre-approves; it does not refuse the rest. Grok's `--allow` does. The deny sets match (`write_tool_path_filters`, one function) and `AdapterCapabilityParityTests` pins that, but a path outside both role roots is reachable on Claude and refused on Grok. The git write fence is the boundary and catches it after the fact. | **open** — closes if the CLI grows a fail-closed allowlist for edits, or if the fence moves in front of the hop (sandbox) |
| L2 | Adapter argv is only tested against semantic doubles | 4 (across the doubles) | `tests/support/claude_argv.py` and `grok_argv.py` interpret argv the way we believe each CLI does. Nothing executes the real binaries, so a vendor changing flag semantics — union vs last-wins, what `acceptEdits` implies — is invisible here. Single-occurrence comma-joined flags remove the worst of it by construction. | **open** — needs a smoke hop against each CLI, outside the unit suite |
| L3 | Class identity contains the model's title | 5 | `finding_identity` is `(path, title, evidence)`; a re-worded finding is a new class. Nothing in a finding payload is more stable. The ledger no longer strands on it (`resolve_seq_state`), so the cost is a re-applied class, not a wedged queue. | **open as a class** |
| L4 | The seq ledger can hold a state no command produced | 2 | `findings.json` on disk is three sets plus two scalars; `resolve_seq_state` is the only reader and drops any marker whose class left the pool. The rung-1 shape is one `pending` slot resolved against the pool, which would make the bad state unwritable rather than unread. | **guarded** |
| L5 | One path list still comes from parsed text | 3 | Every git reader uses `-z` records except `paths_from_diff`, which parses `diff --git a/… b/…` headers because the PR rail has only a patch (`gh pr diff`), never a list. `core.quotepath=false` removes the escaping; a filename containing a space is still ambiguous in that header. | **open** — closes when the PR rail carries a name list beside the patch |
| L6 | The config writer is hand-rolled | 4 | The reader is `tomllib`. The writer stays ours because it preserves comments and unknown keys, which no TOML writer in the stdlib does. `test_written_values_read_back_unchanged` fails when either side moves alone. | **guarded** |
| L7 | One commit carried a whole round of work | 5 | `2b7dde1` landed ~11k lines closing seven classes at once, because that work predates this round and was already green. Bisect over it is coarse. Every commit after it is one class with one test. | **open as a class** — the rule is one defect, one commit, one test |
| L11 | An exemption that outlives the run is a hop-writable input to every later run | 2 | Widening the fence from `.team/work` to `.team/census` made a durable, repo-wide path hop-writable: a `read-only` hop wrote `.team/census/<head>.md`, the fence passed it, and a later run at that HEAD inlined the text into every prompt. Placement cannot fix this — `.team/work` has the same shape, bounded only by being per-slug and ephemeral. The orchestrator now *declares* the paths it writes itself (`_note_orchestrator_write`, path + content id), so the fence bills a hop for what the hop wrote and no path is exempt by name. A hop that changes those bytes is a violation again. | **closed** — the exemption is gone; the property is derived from who wrote it, not from a constant |
| L10 | A warm chain trusts resume semantics nothing here executes | 4 | `--warm` resumes a session only when the whole argv agrees: the key is `(role, runtime, capability, allow, deny)` from `write_tool_path_filters` itself, not a proxy for it, because `_adopt_design_roots` rewrites the roots mid-run and two implementer hops around a replan would otherwise resume with different `--allowedTools` than they opened with. A chain is *consumed* on read and re-armed only at the one exit where the whole hop succeeded, so a hop that raised anywhere — schema, fence, unfinished inspect, all of which leave `result.success` true — hands nothing on. `WarmSessionParityTests` pins that both adapters agree on cold vs warm spelling; `test_warm_and_cold_produce_the_same_artifacts` pins that dropping any link changes nothing. | **guarded** — the equivalence test holds it; only a smoke hop against the real binaries closes L2 underneath it |
| L9 | Carry-vs-fetch is tuned on an unmeasured term | 5 | Inlining an artifact costs its bytes on every turn; making the hop fetch it costs the bytes after the read *plus* a whole turn, and a turn re-sends the context (~54k measured). That comparison assumes one round trip per file, but an agent can batch several reads into one turn, in which case the two strategies converge. `INLINE_ARTIFACT_MAX` (4 KB) and `INLINE_TOTAL_MAX` (12 KB) are set small enough that inlining wins under either assumption; they are not derived from a measurement of batching. `prompt_bytes` / `listed_bytes` in the ledger exist to settle it. | **open** — closes when a before/after on the same review names the real per-read cost |
| L8 | The review surface had no size law | 4 | `ignored_untracked` already says a generated tree is not review material, but it delegates the judgment to the target repo's `.gitignore`: a repo that does not ignore `coverage-html/` handed every hop an 11.1 MB `apply.patch` (2.8M tokens, 442 files, ~90% build output). `budget_sections` / `budget_patch_text` now cap the bytes a hop is *handed*; `porcelain_paths` and the write fence are untouched, and omitted paths stay in `apply-names.txt` / `names.txt` with a header naming them. Real case: 11.1 MB → 524 KB. *Which* bytes survive matters as much as how many: sections arrive in git's lexical status order, so `coverage-html/` preceded `src/` and, being individually under the per-file cap, took the allowance first-come-first-served — a correctly-sized patch of the wrong files (measured: 97% generated, 2 of 6 source modules). `budget_sections` serves the role roots first, smallest-first inside each pass, and lets paths outside those roots take at most half the allowance, because a budget is a cap and not a quota. `ReviewSurfaceBudgetTests` + `ApplySurfaceBudgetTests` pin that the fence does not shrink with the patch; `BudgetOrderingTests` uses *many* files under the per-file cap, the shape a single-huge-file fixture cannot exercise. | **guarded** — the policy is size and location, the only signals available; a generated tree that is small and inside a role root still costs its bytes. Rung 2 is the target repo owning what is reviewable. |

## Not classes, just work

- The debugger/repair rail is opt-in on apply (`--repair`) and is headed for
  its own rail. Do not make it automatic on apply again.
